# DE 8주차 채점기준서 — v2 (PDF v2 기준 · 엄격)

**버전**: v2 (2026-05-09)
**기반**: `DE/8week/문제지/v2/8주차_문제지.pdf`
**v1 대비 변경**: Q7·Q8 spark-submit 강제 부활, Q6 본인이름 print 만점조건 명시, Q9 HTML 본인이름·기수 + Q9·Q10 캡처 `date && hostname` 만점조건 명시

## 채점 원칙

- **0 또는 10점 이진 채점**, 10문제 × 10점 = 100점
- **PDF v2 채점 기준에 명시된 항목만** O/X 판정 근거로 사용
- 채점기준서가 PDF에 없는 추가 조항을 두지 않음 (CLAUDE.md "문제지 우선" 원칙)
- 캡처 요구 문제는 캡처 누락 시 X
- **▲ 과제 제출 양식(폴더 구조 · 파일명) 틀릴 시 10점 감점** (PDF 1쪽 명시)
- **▲ 캡처가 아닌 로그 텍스트로 제출 시 10점 감점** (PDF 1쪽 명시)
- **▲ .docx/.pdf 단독 제출 / 코드 이미지 제출 시 0점 처리** (PDF 1쪽 명시)

## 공통 검증 항목

### A. 캡처 내용 검증 — 파일명만이 아니라 **이미지 내용**이 해당 문제 결과인지
- Q1 `airflow_ui.png`: 파일 존재 + **DAG가 hello_airflow_dag**인지 확인 (s3_download_dag 등 다른 DAG → X)
- Q7 `spark_ui.png`: 파일 존재 + **application UI=`silver_realestate_transform`**인지 확인 (Q5 `Streaming Log Processor` 등 다른 앱 → X)
- 단서: Spark UI 우상단 `{appName} application UI` 텍스트 + 메뉴바 `Structured Streaming` 탭 유무 + Started At 타임스탬프
- 비유: "다른 사람 졸업앨범 사진을 본인 사진으로 제출" — 사진 자체는 멀쩡해도 **누구의 결과물인지가 다르면 X**

### B. Q3 환경 의도 검증 — Airflow 메타DB 시스템 테이블 그대로 SELECT하면 X
- PDF 의도: 별도 PostgreSQL 컨테이너 + 본인 테이블 생성 + Variable로 관리
- 위반 사례: `Variable.get("target_table")="dag"` → Airflow 메타DB의 시스템 `dag` 테이블 SELECT
- 식별 단서: SELECT 결과에 `example_branch_operator`, `example_bash_decorator`, `example_branch_python_operator_decorator` 등 **Airflow 자동 로드 example DAG** 노출 (학생이 만든 데이터 아님)
- docker-compose.yml에 별도 PG 서비스(my_postgres 등) 정의되어 있는지도 보조 단서

### C. 본인 수행 증빙 (PDF v2 본문 명시 + LLM 원샷 방어)
- **Q6 본인이름 print 강제** (PDF v2 본문) — 수집 task 첫 줄에 `print(f'collector={본인이름}, time={datetime.now()}, lawd={LAWD_CD}')`
  - 코드에 본인 이름 collector= 라인 또는 동등 출력 필수
  - 미충족 시 Q6 → X
- **Q9 HTML 본인이름·기수 강제** (PDF v2 본문) — ConfigMap `02-configmap.yaml` index.html 본문에 본인 이름·기수·Q8 결과 요약 포함
  - `capture/browser.png` 화면에 본인 이름·기수 가시적 노출 필수
  - 미충족 시 Q9 → X
- **Q9·Q10 터미널 캡처 `date && hostname` 강제** (AIEngineer 7주차 표준)
  - 대상: `instance_id.png`, `kubectl_get_all.png`, `kubectl_top_pod.png`, `hpa_watch.png`, `hpa_describe.png` 5장
  - 캡처 상단 또는 동일 터미널 세션 내 `date && hostname` 실행 결과 동봉
  - 미충족 시 해당 캡처 무효 → Q9 또는 Q10 → X

---

## Q1 (Q1_1 + Q1_2 모두 충족 필요) — 5점 + 5점 = 10점

### Q1_1 — `hello_airflow_dag.py`
- DAG ID = `hello_airflow_dag`
- BashOperator 사용 + `bash_command`에 `echo "Hello Airflow"` 포함
- `schedule_interval` = 5분 단위 (`*/5 * * * *`)
- Airflow UI Task 성공 캡처 존재
- 캡처 내용 = `hello_airflow_dag` DAG 화면이어야 함 (s3_download_dag 등 다른 DAG → X)

### Q1_2 — `s3_download_dag.py`
- PythonOperator + boto3 (또는 S3Hook) 사용
- Airflow Connection 등록 (aws_default 또는 사용자 정의)
- `/opt/airflow/data/`에 `data.csv` 저장
- Task 성공 + CSV 파일 존재 캡처

---

## Q2 (Q2_1 + Q2_2 모두 충족) — 5점 + 5점 = 10점

### Q2_1 — `analyze_csv.py`
- SparkSession 생성 + `spark.read.csv` 로딩
- `agg`/`groupBy`/`select` 등으로 평균·최댓값·개수 계산
- 결과 콘솔 출력(`show`) 또는 파일 저장

### Q2_2 — `run_spark_job.py`
- **SparkSubmitOperator 사용** (PDF 명시)
- application 파라미터에 Q2_1의 .py 경로
- `schedule_interval` = 5분 단위

---

## Q3 — `db_query_dag.py` — 10점
- PostgresHook 또는 BaseHook.get_connection 사용
- Variable.get으로 테이블명 동적 로딩
- SELECT 결과가 로그/캡처에 노출
- 별도 PostgreSQL 환경 + 본인 테이블 — Airflow 메타DB 시스템 테이블(`dag` 등) 그대로 SELECT 시 X
  - SELECT 결과에 `example_*` Airflow 표준 DAG들이 노출되면 본인 데이터 아닌 시스템 테이블 = X
  - docker-compose.yml에 별도 PG 컨테이너 정의 + CREATE TABLE/INSERT 흔적 있어야 O

---

## Q4 — `apply_udf.py` — 10점
- `@udf` 데코레이터 또는 `udf()` 함수로 UDF 등록
- DataFrame에 UDF 적용 (`withColumn` 등)
- 결과 저장 (`write.csv` / `write.parquet`)

---

## Q5 — `stream_log_processor.py` — 10점
- `spark.readStream.format("socket")` 사용
- ERROR 필터링 + count 집계
- `writeStream.format("console")` 출력

---

## Q6 — Bronze (`bronze_realestate_collect.py`) — 10점
- DAG ID = `bronze_realestate_collect`
- TaskGroup 또는 expand (dynamic task mapping)
- BranchPythonOperator
- boto3.put_object 또는 S3Hook.load_string
- data.go.kr API URL 호출
- `capture/airflow_graph.png` 파일 존재
- `capture/s3_objects.png` 파일 존재 — **AWS S3 콘솔 캡처** (mc ls 등 CLI 출력 무효)
- 수집 task 첫 줄 `print(f'collector={본인이름}, time={datetime.now()}, lawd={LAWD_CD}')` 강제 — 코드에 본인이름 collector= 라인 노출 필수 (PDF v2 본문 명시)

---

## Q7 — Silver — 10점

### O 기준 (모두 충족)
- DAG ID = `silver_realestate_transform`
- ExternalTaskSensor (Q6 DAG 완료 대기)
- **(v2) SparkSubmitOperator 또는 BashOperator + spark-submit 사용** — PythonOperator + python 실행 → X
- udf 등록 2회 이상 (`@udf` 또는 `udf()`)
- approxQuantile 또는 percentile_approx (IQR 계산)
- `write.parquet`
- `compression='snappy'` 또는 partitionBy
- `capture/spark_ui.png` 파일 존재 + 내용이 silver Spark Job 실행 화면 (우상단 `application UI=silver_realestate_transform`. Q5 `Streaming Log Processor` 등 다른 앱 UI → X)
- `capture/s3_silver.png` 파일 존재

### X 사례
- ❌ PythonOperator로 직접 SparkSession 생성 후 transform 함수 호출
- ❌ `subprocess.run(["python", "..."])` 로 Spark 스크립트 실행
- ❌ BashOperator로 `python silver_spark.py` 실행 (spark-submit 미사용)

---

## Q8 — Gold — 10점

### O 기준 (모두 충족)
- DAG ID = `gold_realestate_aggregate`
- ExternalTaskSensor (Q7 DAG 완료 대기)
- **(v2) SparkSubmitOperator 또는 BashOperator + spark-submit 사용** — PythonOperator + python 실행 → X
- `spark.sql` 또는 `groupBy.agg` (5개 집계 코드 존재)
- `lag` 또는 `Window.partitionBy` (window function)
- `write.jdbc` 또는 PostgresHook
- 5개 테이블명 모두 코드에 존재 (`district_avg`, `top10`, `size_dist`, `age_avg`, `mom_change`)
- `capture/postgres_tables.png` 파일 존재
- `capture/postgres_select.png` 파일 존재

### Q6·Q7·Q8 공통 환경
- 통합 폴더 `Q6_7_8_realestate/` + `docker-compose.yml` 필수
- docker-compose.yml 누락 시 Q6·Q7·Q8 모두 0점 (PDF 명시)

---

## Q9 — EC2 + k3s Nginx 배포 — 10점
- 5개 manifest 파일 모두 존재 (`01-namespace` ~ `05-ingress`)
- Namespace, ConfigMap, Deployment, Service, Ingress 리소스 각 1개 이상
- `replicas: 2`
- `livenessProbe` + `readinessProbe` 모두 존재
- `resources.requests` + `resources.limits` 모두 존재
- `capture/instance_id.png` + `capture/kubectl_get_all.png` + `capture/browser.png` 파일 존재
- ConfigMap `02-configmap.yaml` index.html 본문에 본인 이름·기수·Q8 결과 요약 포함 — `capture/browser.png` 화면에 본인 이름·기수 가시적 노출 필수 (PDF v2 본문 명시)
- `capture/instance_id.png` · `capture/kubectl_get_all.png` 캡처 상단에 `date && hostname` 출력 동봉 (PDF v2 명시 · AIEngineer 7주차 표준)

---

## Q10 — HPA 오토스케일링 — 10점
- `07-hpa.yaml`: `apiVersion: autoscaling/v2`
- `minReplicas` + `maxReplicas` 정의
- `averageUtilization` 또는 `averageValue` 정의
- `load-test.sh` 파일 존재
- `capture/kubectl_top_pod.png` + `capture/hpa_watch.png` + `capture/hpa_describe.png` 파일 존재
- 위 3개 캡처 상단에 `date && hostname` 출력 동봉 (PDF v2 명시 · AIEngineer 7주차 표준)
