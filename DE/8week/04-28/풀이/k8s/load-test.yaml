apiVersion: v1
kind: Pod
metadata:
  name: busybox-load-test
spec:
  containers:
  - name: busybox
    image: busybox:1.36
    command:
    - /bin/sh
    - -c
    - "while true; do wget -q -O- http://nginx-hpa-deployment.default.svc.cluster.local; done"
  restartPolicy: Never
