\pset pager off
\echo === gold_realestate_district_avg ===
SELECT * FROM gold_realestate_district_avg LIMIT 5;
\echo === gold_realestate_top10 ===
SELECT * FROM gold_realestate_top10 LIMIT 5;
\echo === gold_realestate_size_dist ===
SELECT * FROM gold_realestate_size_dist LIMIT 5;
\echo === gold_realestate_age_avg ===
SELECT * FROM gold_realestate_age_avg LIMIT 5;
\echo === gold_realestate_mom_change ===
SELECT * FROM gold_realestate_mom_change LIMIT 5;
